export type ThemePreviewProps = {
  primaryColor: string;
  secondaryColor: string;
  fontSize?: string;
  borderWidth: string;
  tertiaryColor: string;
  selected?: boolean;
  onClick?: () => void;
};
